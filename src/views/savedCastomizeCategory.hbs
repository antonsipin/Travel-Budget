<div class="container">
  <div class="equally">

    <h2 id="gr-word">{{newTrip.name}} trip</h2><br />
    <h2 class="useraccount giveitaname">Category {{categoryName}} successfully saved!</h2>

    <div class="d-flex justify-content-center">
      <form class="account">
        <button formaction="/newtrip/addcategory" name="tripName" formmethod="post" type="submit"
          value="{{newTrip.name}}" class="btn btn-primary green lighten-3">Add category</button>
      </form>
    </div> <br />

    {{#each castomCostArr}}
    <div class="account">
      <h3>
        <a> {{this.name}}, </a>
        <a> you spent ${{this.cost}}.00 USD </a> <br />
      </h3>
    </div>
    {{/each}}

  </div>

</div>
